<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhs.mapper.TelnetPermissionMapper">
  <resultMap id="BaseResultMap" type="com.zhs.pojo.TelnetPermission">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="resUrl" jdbcType="VARCHAR" property="resurl" />
    <result column="type" jdbcType="INTEGER" property="type" />
    <result column="parentId" jdbcType="INTEGER" property="parentid" />
    <result column="sort" jdbcType="INTEGER" property="sort" />
  </resultMap>


  <select id="queryAll" resultType="com.zhs.pojo.TelnetPermission">
      	SELECT id,name,parentId,resUrl,type,sort
		FROM telnet_permission
		ORDER BY sort ASC
  </select>

    <select id="loadUserResources" parameterType="java.util.HashMap" resultType="com.zhs.pojo.TelnetPermission">
        SELECT re.id,re.name,re.parentId,re.resUrl
        FROM telnet_permission re LEFT JOIN telnet_role_permission rr
        ON re.id = rr.resourcesId
        LEFT JOIN telnet_user_role ur
        ON rr.roleId =ur.roleId
        WHERE ur.userId=#{userid}
        <if test="type != null">
            AND re.type= #{type}
        </if>
        GROUP BY re.id
        ORDER BY re.sort ASC
    </select>

    <select id="findResourceByParentId" resultType="com.zhs.pojo.TelnetPermission">
        select * from  telnet_permission where parentid=#{parentid}
    </select>
</mapper>